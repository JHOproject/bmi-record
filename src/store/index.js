import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex) // 告訴 Vue 使用 vuex

// 定義一個新的 Vue Store
/* eslint-disable */ 
export default new Vuex.Store({
  strict: true,
  state: {
  },
  mutations: {
  },
  actions: {
  },
  getters: {
  }
})